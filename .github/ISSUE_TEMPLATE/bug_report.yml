name: Bug report
description: Something isn't working in one of the apps.
title: "[Bug]: "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for the report! Please **don't include secrets or sensitive source material** —
        no API keys or tokens, no raw `.tm7` threat model, and no proprietary code. A sanitized
        snippet or screenshot is enough.
  - type: dropdown
    id: product
    attributes:
      label: App
      description: Which app is this about?
      options:
        - Threat Model Reviewer
        - shot2code
        - Other / not sure
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: App version
      description: Shown in the app (title bar or Settings), or on the release you downloaded.
      placeholder: "1.0.0"
    validations:
      required: true
  - type: dropdown
    id: install
    attributes:
      label: How did you install it?
      options:
        - Installer (MSI)
        - Installer (Setup.exe / NSIS .exe)
        - Portable (zip)
        - MSIX
        - Built from source
        - Other / not sure
    validations:
      required: true
  - type: input
    id: os
    attributes:
      label: Windows version
      placeholder: "Windows 11 23H2 (x64)"
    validations:
      required: true
  - type: textarea
    id: what-happened
    attributes:
      label: What happened?
      description: What did you do, what did you expect, and what happened instead?
    validations:
      required: true
  - type: textarea
    id: repro
    attributes:
      label: Steps to reproduce
      placeholder: |
        1. Open '...'
        2. Click '...'
        3. See error
    validations:
      required: false
  - type: textarea
    id: logs
    attributes:
      label: Error text / screenshots
      description: Paste any error message or attach a screenshot (drag & drop).
      render: text
    validations:
      required: false
  - type: checkboxes
    id: confirm
    attributes:
      label: Confirmation
      options:
        - label: This report contains no secrets and no sensitive source material (no API keys or tokens, no raw threat-model file, no proprietary code).
          required: true
